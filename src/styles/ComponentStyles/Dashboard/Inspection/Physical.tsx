import { styled } from "styled-components";
import { SubsectionStyles } from "../HouseDetailsStyles";
import { LargeBtnStyles } from "../../HeaderStyles";

export const CheckAvailabilityStyles = styled(SubsectionStyles)`
  .flex {
    display: flex;
    align-items: center;
    gap: 0.625rem;
  }
`;
